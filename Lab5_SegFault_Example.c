#include <stdio.h>#include <string.h>void tester(int* c, int k) {	printf("x[%d] = %d\n", k, c[k]);	c[9999]=1309; //by changing the size of the array, this is now valid}int main(int argc, char* argv[]) {	int i = 0, k; //deleted unused variable j	//made array larger to accommodate the numbers that it asked for	//between 0 and 9999	int x[10000]; 		while(i < 10000){ //change length of loop to reflect new length of array		x[i] = i;		i++;	}	printf("Enter an integer between 0 to 9999: ");	fflush(stdout);	scanf("%d",&k);		tester(x, k);}